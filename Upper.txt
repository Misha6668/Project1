tx = "ab"


def fun(string):
    string = list(string)
    for i in range(0, len(string), 1):
         
         elem = chr(ord(string[i]) - 32)
         string.remove(string[i])
         string.insert(i, elem)
         
    return string
        
        
        
result = fun(tx)

print(fun)